# dbc/

Finalized DBC files for this project, promoted from the skill's
`decoding-output/<application>/<application>.dbc` output once a signal set is
confirmed. This is the stable, version-controlled DBC used to map live/logged
CAN data for the ML and display work, as opposed to the transient, gitignored
`decoding-output/` working directory.
