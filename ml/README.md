# ml/

Maps CAN logs to the DBC in `dbc/` and builds models for performance analysis.

- `ml/data/` — CAN logs decoded/mapped against the DBC (gitignored; see `.gitignore`)
- `ml/models/` — training scripts and trained model artifacts
- `ml/notebooks/` — exploratory analysis
